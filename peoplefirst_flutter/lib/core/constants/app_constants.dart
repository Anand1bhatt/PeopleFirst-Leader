abstract class AppConstants {
  // Spacing
  static const double spacingXs = 4.0;
  static const double spacingSm = 8.0;
  static const double spacingMd = 12.0;
  static const double spacingLg = 16.0;
  static const double spacingXl = 20.0;
  static const double spacingXxl = 24.0;

  // Border radius
  static const double radiusSm = 8.0;
  static const double radiusMd = 12.0;
  static const double radiusLg = 16.0;
  static const double radiusXl = 20.0;
  static const double radiusFull = 999.0;

  // Card
  static const double cardPadding = 16.0;
  static const double cardRadius = 16.0;

  // Bottom nav height
  static const double bottomNavHeight = 64.0;

  // Toast
  static const Duration toastDuration = Duration(milliseconds: 2600);
  static const double toastBottomOffset = 96.0;

  // AI response delay
  static const Duration aiResponseDelay = Duration(milliseconds: 1500);

  // Attendance timer tick
  static const Duration attendanceTick = Duration(seconds: 30);
}
